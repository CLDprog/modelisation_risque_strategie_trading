# Python generated files
__pycache__/
*.py[cod]
*$py.class

# VS Code and editor backups
.vscode/
*.swp
*.tmp
*.log

# Data files and runtime artifacts — TOUT le store runtime reste hors dépôt
# (Parquet analytics + raw, payloads d'évidence, statuts, alertes, manifests, logs)
data/
logs/

env/
venv/
.python-version

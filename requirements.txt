# Vol Risk Infrastructure — dépendances (versions épinglées sur l'environnement validé).
# Installer : pip install -r requirements.txt

# --- Connectivité broker : IBKR Client Portal Web API (REST + WebSocket) ---
ibind==0.1.23
websocket-client==1.9.0

# --- Calcul numérique ---
numpy==2.4.1
pandas==2.3.3
scipy==1.17.0
pyarrow==23.0.1

# --- Stockage (SQLite via SQLAlchemy + Parquet via pyarrow) ---
SQLAlchemy==2.0.50

# --- Configuration ---
PyYAML==6.0.3
python-dotenv==1.2.2
pydantic==2.13.4

# --- Logging / observabilité ---
loguru==0.7.3
rich==15.0.0

# --- Frontend (dashboard Dash) ---
dash==4.1.0
dash-bootstrap-components==2.0.4
plotly==6.7.0
matplotlib==3.10.9

# --- Tests ---
pytest==9.0.3
pytest-cov==7.1.0
